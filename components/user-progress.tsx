type Props = {
  activeCourse: any
  hearts: number
  points: number
  hasActiveSubscription: Boolean
}

export const UserProgress = ({}: Props) => {
  return(
    <div>
      User Progress
    </div>
  )
}